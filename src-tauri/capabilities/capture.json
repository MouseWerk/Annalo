{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Permissions of the quick-capture window (it talks to the shell through app commands only; it may resize itself to fit its content and ask for the focus again when the system kept another window in front)",
  "windows": ["capture"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-size",
    "core:window:allow-set-focus"
  ]
}
