{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Permissions of the quick-capture window (it talks to the shell through app commands only; it may resize itself to fit its content)",
  "windows": ["capture"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-size"
  ]
}
