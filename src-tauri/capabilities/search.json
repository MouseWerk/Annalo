{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "search",
  "description": "Permissions of the quick-search window (it opens results through app commands only; events for `search://shown`)",
  "windows": ["search"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
