{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows",
  "description": "Windows: the app draws its own title bar (Settings → Darstellung). The tab bar moves the window (data-tauri-drag-region, double-click maximizes) and the UI has its own minimize, maximize and close buttons",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close"
  ]
}
