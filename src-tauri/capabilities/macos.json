{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos",
  "description": "macOS: the tab bar and the top of the ribbon sit in the title bar (overlay) and move the window (data-tauri-drag-region)",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}
